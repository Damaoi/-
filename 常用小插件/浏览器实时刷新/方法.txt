1、安装淘宝镜像 原装也可以

2、安装BrowserSync：npm install -g browser-sync

3、启动 BrowserSync：// --files 路径是相对于运行该命令的项目（目录） 
browser-sync start --server --files "**/*.css,**/*.html,**/*.js	
同时检测两个文件用逗号隔开
 

根目录下必须有index.html文件
然后在浏览器改你要访问的文件夹下的html文件

手机端同步的话 和pc端的url是相同的

微信二维码打开的话 把localhost换成本机ip